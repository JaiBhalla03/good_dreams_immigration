import React from 'react';
import heroImage from '../images/heroImage.png'

const Hero = () => {
    return (
        <div className={'h-screen'}>
          {/*<Image src={heroImage} alt={''} width={200}/>*/}
        </div>
    );
};

export default Hero;